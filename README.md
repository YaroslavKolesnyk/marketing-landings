# 🪄 Landing Collection — тестове завдання

## Опис
Це тестовий проєкт, розроблений на **Next.js** (React framework), який демонструє створення декількох адаптивних маркетингових лендингів з анімаціями, плавними переходами та сучасною версткою.

## ⚙️ Технології

- **Next.js 14** — фреймворк для React з підтримкою SSR та статичної генерації (Static Export)
- **TypeScript** — типізація компонентів
- **Tailwind CSS** — швидке створення адаптивного інтерфейсу
- **AOS (Animate on Scroll)** — анімації при прокрутці
- **React Icons / Lucide React** — набір іконок для UI
- **Facebook Pixel** — базове підключення аналітики для відстеження взаємодії користувачів

---


⚠️ У репозиторії вже є готова збірка (`/out`), тому **білдити повторно не обов’язково**.  
Папка **/out** — це результат статичної генерації Next.js (команда `next export`),  
яка містить усі HTML, CSS і JS файли готових лендингів. Саме цю збірку можна відкрити напряму або запустити через локальний сервер.

---

## 🚀 Запуск проєкту локально

```bash
npm install
npx serve out


